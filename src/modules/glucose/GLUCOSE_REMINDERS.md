# 📝 ระบบแจ้งเตือนบันทึกค่าน้ำตาลประจำวัน

## 🎯 วัตถุประสงค์

ส่งข้อความแจ้งเตือนให้ผู้ใช้ที่ยังไม่ได้บันทึกค่าน้ำตาลในวันนั้น เพื่อสร้างนิสัยการบันทึกข้อมูลสุขภาพอย่างสม่ำเสมอ

## ⏰ กำหนดการ

- **เวลา:** ทุกวัน 07:00 น. (เวลาไทย)
- **Cron Pattern:** `0 7 * * *`
- **เงื่อนไข:** ส่งเฉพาะผู้ใช้ที่ยังไม่มี `GlucoseLog` ในวันนั้น

## 💬 ข้อความแจ้งเตือน

ระบบมีข้อความแบบสุ่ม 10 แบบ เพื่อไม่ให้น่าเบื่อ:

1. สวัสดีตอนเช้า ☀️ อย่าลืมวัดและบันทึกค่าน้ำตาลวันนี้นะคะ
2. อรุณสวัสดิ์ค่ะ 🌅 มาบันทึกค่าน้ำตาลกันเถอะ เพื่อสุขภาพที่ดีขึ้น
3. ตื่นแล้วหรือยังคะ 😊 อย่าลืมวัดค่าน้ำตาลตอนเช้านะ
4. สวัสดีค่ะ 💙 วันนี้อย่าลืมบันทึกค่าน้ำตาลด้วยนะคะ
5. เช้าวันใหม่ ☕ มาเริ่มต้นด้วยการวัดค่าน้ำตาลกันเถอะ
6. หวัดดีตอนเช้า 🌤️ ถึงเวลาบันทึกค่าน้ำตาลแล้วค่ะ
7. อรุณสวัสดิ์ 🌻 อย่าลืมดูแลสุขภาพด้วยการบันทึกค่าน้ำตาลนะคะ
8. สวัสดีค่ะ 😊 วันนี้บันทึกค่าน้ำตาลแล้วหรือยังคะ
9. ตื่นมาแล้วอย่าลืมวัดค่าน้ำตาลนะคะ 💪 เพื่อติดตามสุขภาพของเรา
10. เช้าวันใหม่ที่สดใส ☀️ มาบันทึกค่าน้ำตาลกันเถอะค่ะ

## 🔄 Logic การทำงาน

```typescript
1. ดึงข้อมูล User ทั้งหมด
2. ตรวจสอบว่า User แต่ละคนมี GlucoseLog ในวันนี้หรือไม่
3. กรอง User ที่ยังไม่ได้บันทึก
4. สุ่มข้อความแจ้งเตือน
5. ส่งข้อความผ่าน LINE Push Message
6. บันทึก log สำเร็จ/ล้มเหลว
```

## 📊 ตัวอย่าง Output

```
🔔 Running daily glucose reminders...
👥 Found 150 total users
📝 45 users haven't logged glucose today
✅ Sent reminder to user 1
✅ Sent reminder to user 5
✅ Sent reminder to user 12
...
📊 Summary: 45 sent, 0 failed
```

## 🧪 การทดสอบ

### ทดสอบด้วยตนเอง

```bash
bun run test:reminders
```

### ทดสอบในโค้ด

```typescript
import { sendDailyGlucoseReminders } from './modules/glucose/glucose-reminder.service'

await sendDailyGlucoseReminders()
```

## 🎨 Design Principles

1. **เป็นกันเอง** - ใช้ภาษาที่อบอุ่น ไม่เป็นทางการจนเกินไป
2. **หลากหลาย** - สุ่มข้อความเพื่อไม่ให้น่าเบื่อ
3. **เบา** - ใช้ Text Message ธรรมดา ไม่หนักเหมือน Flex Message
4. **ตรงเวลา** - ส่งตอนเช้า 7 โมง เวลาที่เหมาะสมสำหรับการวัดค่าน้ำตาล
5. **ไม่รบกวน** - ส่งเฉพาะคนที่ยังไม่ได้บันทึก

## 🔧 การปรับแต่ง

### เปลี่ยนเวลาแจ้งเตือน

แก้ไขใน `src/cron/appointment-reminders.cron.ts`:

```typescript
// เปลี่ยนเป็น 8 โมงเช้า
cron.schedule('0 8 * * *', async () => {
  await sendDailyGlucoseReminders()
})
```

### เพิ่มข้อความใหม่

แก้ไขใน `src/modules/glucose/glucose-reminder.service.ts`:

```typescript
const reminderMessages = [
  'สวัสดีตอนเช้า ☀️ อย่าลืมวัดและบันทึกค่าน้ำตาลวันนี้นะคะ',
  // เพิ่มข้อความใหม่ที่นี่
  'ข้อความใหม่ของคุณ 🎉'
]
```

### เปลี่ยนเงื่อนไขการส่ง

ปัจจุบันส่งเฉพาะคนที่ยังไม่ได้บันทึกในวันนั้น หากต้องการเปลี่ยนเงื่อนไข:

```typescript
// ส่งให้ทุกคน (ไม่แนะนำ)
const usersToNotify = allUsers

// ส่งเฉพาะคนที่ไม่ได้บันทึกมา 2 วัน
const twoDaysAgo = dayjs().subtract(2, 'day').startOf('day')
// ... เพิ่ม logic ตรวจสอบ
```

## 📈 Best Practices

1. ✅ ส่งเฉพาะคนที่ต้องการ (ยังไม่ได้บันทึก)
2. ✅ ใช้ข้อความที่หลากหลาย
3. ✅ ส่งในเวลาที่เหมาะสม (เช้า)
4. ✅ จัดการ error อย่างเหมาะสม
5. ✅ Log ผลการส่งเพื่อติดตาม

## 🚫 สิ่งที่ควรหลีกเลี่ยง

1. ❌ ส่งให้ทุกคนทุกวัน (รบกวน)
2. ❌ ใช้ข้อความเดิมซ้ำๆ (น่าเบื่อ)
3. ❌ ส่งในเวลาที่ไม่เหมาะสม (กลางคืน)
4. ❌ ไม่จัดการ error
5. ❌ ส่งบ่อยเกินไป (วันละหลายครั้ง)

## 🔍 Troubleshooting

### ไม่ได้รับแจ้งเตือน

1. ตรวจสอบว่า cron job ทำงานหรือไม่
2. ตรวจสอบว่ามี GlucoseLog ในวันนี้หรือยัง
3. ตรวจสอบ LINE_CHANNEL_ACCESS_TOKEN
4. ตรวจสอบว่า User เป็นเพื่อนกับ Bot แล้ว

### ได้รับแจ้งเตือนทั้งที่บันทึกแล้ว

1. ตรวจสอบ timezone ของ database
2. ตรวจสอบ `recordedAt` ใน GlucoseLog
3. ตรวจสอบ logic การกรอง User

## 💡 ไอเดียเพิ่มเติม

- เพิ่มการตั้งค่าเปิด/ปิดการแจ้งเตือนใน UserSetting
- ปรับเวลาแจ้งเตือนตามความต้องการของแต่ละคน
- ส่งสถิติการบันทึกประจำสัปดาห์
- แจ้งเตือนเมื่อค่าน้ำตาลผิดปกติ
- Gamification: ให้คะแนนเมื่อบันทึกต่อเนื่อง
