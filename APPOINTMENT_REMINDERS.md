# 🔔 ระบบแจ้งเตือนอัตโนมัติ (Automated Reminders)

ระบบแจ้งเตือนอัตโนมัติผ่าน LINE สำหรับนัดหมายและการบันทึกค่าน้ำตาล

## 📋 คุณสมบัติ

### 1. แจ้งเตือนล่วงหน้า 3 วัน (Appointment)
- ⏰ เวลา: ทุกวัน 09:00 น.
- 📅 แจ้งเตือนนัดหมายที่จะถึงในอีก 3 วัน
- 💙 ข้อความ: "อย่าลืมเตรียมตัวนะคะ"
- 🎨 รูปแบบ: Flex Message สวยงาม

### 2. แจ้งเตือนวันนัดหมาย (Appointment)
- ⏰ เวลา: ทุกวัน 05:00 น.
- 📅 แจ้งเตือนนัดหมายในวันนั้น
- 💙 ข้อความ: "ขอให้เดินทางปลอดภัยนะคะ"
- 🎨 รูปแบบ: Flex Message สวยงาม

### 3. แจ้งเตือนบันทึกค่าน้ำตาล (Glucose)
- ⏰ เวลา: ทุกวัน 07:00 น.
- 📝 แจ้งเตือนผู้ใช้ที่ยังไม่ได้บันทึกค่าน้ำตาลในวันนั้น
- 💬 ข้อความ: แบบสุ่ม 10 แบบ พูดเป็นกันเอง
- 📱 รูปแบบ: Text Message ธรรมดา

## 🎨 Message Design

### Appointment Reminders (Flex Message)

ข้อความแจ้งเตือนนัดหมายออกแบบให้สวยงามและอ่านง่าย:

```
┌─────────────────────────────┐
│ 🔔 แจ้งเตือนนัดหมายล่วงหน้า │ (สีฟ้า)
├─────────────────────────────┤
│ อีก 3 วัน                   │
│ จันทร์ที่ 30/10/2568        │
│ 09:00 - 10:00 น.            │
├─────────────────────────────┤
│ 🏥 โรงพยาบาลรามาธิบดี       │
│ 👨‍⚕️ นพ.สมชาย ใจดี          │
│ 📋 ตรวจเบาหวาน              │
├─────────────────────────────┤
│ อย่าลืมเตรียมตัวนะคะ 💙     │
└─────────────────────────────┘
```

### Glucose Reminders (Text Message)

ข้อความแจ้งเตือนบันทึกค่าน้ำตาลแบบสุ่ม 10 แบบ:

1. "สวัสดีตอนเช้า ☀️ อย่าลืมวัดและบันทึกค่าน้ำตาลวันนี้นะคะ"
2. "อรุณสวัสดิ์ค่ะ 🌅 มาบันทึกค่าน้ำตาลกันเถอะ เพื่อสุขภาพที่ดีขึ้น"
3. "ตื่นแล้วหรือยังคะ 😊 อย่าลืมวัดค่าน้ำตาลตอนเช้านะ"
4. "สวัสดีค่ะ 💙 วันนี้อย่าลืมบันทึกค่าน้ำตาลด้วยนะคะ"
5. "เช้าวันใหม่ ☕ มาเริ่มต้นด้วยการวัดค่าน้ำตาลกันเถอะ"
6. "หวัดดีตอนเช้า 🌤️ ถึงเวลาบันทึกค่าน้ำตาลแล้วค่ะ"
7. "อรุณสวัสดิ์ 🌻 อย่าลืมดูแลสุขภาพด้วยการบันทึกค่าน้ำตาลนะคะ"
8. "สวัสดีค่ะ 😊 วันนี้บันทึกค่าน้ำตาลแล้วหรือยังคะ"
9. "ตื่นมาแล้วอย่าลืมวัดค่าน้ำตาลนะคะ 💪 เพื่อติดตามสุขภาพของเรา"
10. "เช้าวันใหม่ที่สดใส ☀️ มาบันทึกค่าน้ำตาลกันเถอะค่ะ"

**หมายเหตุ:** ระบบจะส่งแจ้งเตือนเฉพาะผู้ใช้ที่ยังไม่ได้บันทึกค่าน้ำตาลในวันนั้น

## 🗂️ โครงสร้างไฟล์

```
src/
├── cron/
│   ├── appointment-reminders.cron.ts  # Cron job configuration
│   ├── test-reminders.ts              # ไฟล์ทดสอบ
│   └── README.md                      # เอกสารอธิบาย
└── modules/
    ├── appointment/
    │   └── appointment-notification.service.ts  # Logic แจ้งเตือนนัดหมาย
    └── glucose/
        └── glucose-reminder.service.ts          # Logic แจ้งเตือนบันทึกค่าน้ำตาล
```

## 🚀 การใช้งาน

### เริ่มต้นระบบ

ระบบจะเริ่มทำงานอัตโนมัติเมื่อรัน API server:

```bash
bun run dev
```

คุณจะเห็นข้อความ:
```
✅ Reminder cron jobs initialized
   - 3-day appointment reminders: Every day at 09:00 (Asia/Bangkok)
   - Same-day appointment reminders: Every day at 05:00 (Asia/Bangkok)
   - Daily glucose reminders: Every day at 07:00 (Asia/Bangkok)
```

### ทดสอบการส่งแจ้งเตือน

```bash
bun run test:reminders
```

## 📊 Database Logging

ระบบจะบันทึก log ทุกครั้งที่ส่งแจ้งเตือนใน `NotificationLog`:

```typescript
{
  id: 1,
  userId: 123,
  appointmentId: 456,
  notificationType: "3_DAY_REMINDER", // หรือ "SAME_DAY_REMINDER"
  sentAt: "2024-10-27T09:00:00Z",
  success: true,
  errorMessage: null,
  retryCount: 0
}
```

### ป้องกันการส่งซ้ำ

ใช้ unique constraint `@@unique([appointmentId, notificationType])` เพื่อป้องกันการส่งแจ้งเตือนซ้ำ

## 🔧 การปรับแต่ง

### เปลี่ยนเวลาแจ้งเตือน

แก้ไขใน `src/cron/appointment-reminders.cron.ts`:

```typescript
// แจ้งเตือนล่วงหน้า 3 วัน เวลา 08:00 น.
cron.schedule('0 8 * * *', async () => {
  await sendThreeDayReminders()
})

// แจ้งเตือนวันนัดหมาย เวลา 06:00 น.
cron.schedule('0 6 * * *', async () => {
  await sendSameDayReminders()
})
```

### เปลี่ยนจำนวนวันล่วงหน้า

แก้ไขใน `src/modules/appointment/appointment-notification.service.ts`:

```typescript
// แจ้งเตือนล่วงหน้า 7 วัน
const sevenDaysFromNow = dayjs().add(7, 'day').startOf('day')
```

### ปรับแต่ง Flex Message

แก้ไขฟังก์ชัน `sendAppointmentFlexMessage()` ใน `appointment-notification.service.ts`

## 📝 Cron Pattern

```
* * * * *
│ │ │ │ │
│ │ │ │ └─ วัน (0-6, 0=อาทิตย์)
│ │ │ └─── เดือน (1-12)
│ │ └───── วันที่ (1-31)
│ └─────── ชั่วโมง (0-23)
└───────── นาที (0-59)
```

ตัวอย่าง:
- `0 9 * * *` = ทุกวัน 09:00 น.
- `0 5 * * *` = ทุกวัน 05:00 น.
- `0 9 * * 1` = ทุกวันจันทร์ 09:00 น.
- `0 9 1 * *` = วันที่ 1 ของทุกเดือน 09:00 น.

## 🐛 Troubleshooting

### ไม่ได้รับแจ้งเตือน

1. ตรวจสอบว่า cron job ทำงานหรือไม่:
   ```bash
   # ดู log ใน console
   ⏰ [CRON] Running 3-day appointment reminders at 09:00
   ```

2. ตรวจสอบข้อมูลนัดหมาย:
   ```sql
   SELECT * FROM "Appointment" 
   WHERE "appointmentDate" >= NOW() 
   AND "appointmentDate" <= NOW() + INTERVAL '3 days';
   ```

3. ตรวจสอบ NotificationLog:
   ```sql
   SELECT * FROM "NotificationLog" 
   WHERE "appointmentId" = [ID] 
   AND "notificationType" = '3_DAY_REMINDER';
   ```

4. ทดสอบด้วยตนเอง:
   ```bash
   bun run test:reminders
   ```

### Error: LINE API Failed

ตรวจสอบ:
- `LINE_CHANNEL_ACCESS_TOKEN` ใน `.env`
- User มี `lineUserId` ที่ถูกต้อง
- LINE Bot เป็นเพื่อนกับ User แล้ว

## 🎯 Best Practices

1. ✅ ตรวจสอบ timezone ให้ถูกต้อง (`Asia/Bangkok`)
2. ✅ บันทึก log ทุกครั้งที่ส่งแจ้งเตือน
3. ✅ จัดการ error และ retry
4. ✅ ป้องกันการส่งซ้ำด้วย unique constraint
5. ✅ ทดสอบก่อน deploy production

## 📚 เอกสารเพิ่มเติม

- [node-cron Documentation](https://github.com/node-cron/node-cron)
- [LINE Flex Message Simulator](https://developers.line.biz/flex-simulator/)
- [dayjs Documentation](https://day.js.org/)
